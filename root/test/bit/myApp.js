/*global define, describe, it, expect */
define([
	"src/sonoptmanager/sonoptmanagerView"
], function (myApp) {
    'use strict';

    describe("myApp", function () {

        it("myApp should be defined", function () {
            expect(myApp).not.to.be.undefined;
        });
    });

});